user nginx;

events {
    worker_connections 1000;
}

http {
    server {
        listen 80;
        server_name localhost;
        access_log  off;

        location /api {
            proxy_pass http://server:8000;
        }
    }
}
